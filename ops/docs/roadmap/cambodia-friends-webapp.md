# Cambodia, friend play, and web-app continuation

## Delivered

- Cambodia has its own `ouk-chaktrang` variant and home card, native Khmer name, asymmetric royal setup, opening leaps, permanent king-leap restrictions, sixth-rank promotion, plain board, piece names, and an original SVG collection. The catalog now has 21 board adapters, with Cambodia explicitly in local/bot preview while competitive verification is unfinished. Published digital counting and three teaching positions are now implemented; see `ouk-counting-profile.md`.
- Original Higgsfield/Blender collection: six carved shapes in two materials. The editable `.blend`, portable `.glb`, and rendered preview are committed with provenance in `ops/assets/khmer/README.md`. Optional interactive 3D supports tapping moves, orbiting, zooming, board rotation, and themes. It loads only on demand, remembers the view, reuses geometry, avoids redraws for clock-only updates, and retains the accessible 2D option.
- Classic chess and seven modern variants now have their own original ivory/walnut Blender collection. The shared 3D renderer offers real porcelain/slate materials, an adjustable angled camera with reset, small unboxed edge coordinates, move markers, promotion rings, and safe 2D recovery. Preferences remain specific to each game. See `ops/assets/classic/README.md`.
- Setup uses tactile buttons for clocks and sides, and a disclosure with real choice buttons for long bot/family lists. Appearance uses visual presets. The 2D board has a single tab entry and arrow-key square navigation.
- Friend invites connect two browsers through authoritative room actions: assigned seats, legal moves and drops, server clocks, agreed draws, resignation, rejoining, spectator views, and private player messages. Snapshots have monotonically increasing revisions so slower responses cannot overwrite newer state.
- Coordinated rematches keep the invite and both players, require mutual acceptance, swap sides, and reset the board/clocks. Per-game IDs reject delayed actions from an earlier game. Offers can be cancelled; chat keeps participant identity across side changes. Connection feedback covers offline/reconnecting/unavailable states, bounded requests, automatic recovery, manual retry, and last-seen friend presence. Server clocks continue during disconnection.
- Production rooms use the existing `GAME_ROOM_DO` binding, serialized storage transitions, hashed seat credentials, and seven-day expiry with a storage alarm. The bounded in-memory fallback is restricted to development/test. Production without the binding returns unavailable instead of pretending a local room is online.
- Installable manifest, regular/maskable icons, browser install prompt, and a downloadable offline play pack (about 22 MiB). A public static shell reuses all 21 playable board adapters, local bots including Stockfish, themes, and the two 3D collections. Cold launches work after download. Build-generated hashes verify every file before replacing a working pack. Private pages, APIs, RSC responses, room state, and account information are excluded. See `offline-play-pack.md`.

## Validation

Latest offline continuation: 384 unit/domain/API cases pass, including eight service-worker checks for cold navigation, private-data exclusions, path traversal, corrupt updates, concurrent requests, and storage eviction. A real Chrome profile downloaded the production pack, closed, and reopened with networking disabled: all 21 playable boards opened, Cambodia's opening king leap worked, classic produced an offline bot reply, and both 3D collections loaded without runtime errors. Desktop/mobile download controls and 320/390-pixel board layouts were inspected. Production build generates 209 pages and the verified asset manifest.

Latest tabletop continuation: 376 unit/domain/API tests pass, including actual GLB structure/scale and camera-framing checks. The board now has a bevelled walnut case, brass edging, raised frame, feet, wood grain, and cast shadows on a table. One angled camera supports orbit, zoom, and reset; camera preset buttons are removed. Coordinates use small unboxed text in both renderers. Current desktop/mobile audits verify Cambodia and classic raycast moves, orbit/zoom/reset, transparent coordinates, and 320/390-pixel layouts, with no runtime errors. Prior collection audits cover all nine enabled games, saved materials, rotation, castling, promotion, Crazyhouse drops, failed model downloads, and WebGL context loss. Keyboard play remains in the 2D view.

Previous friend-lifecycle continuation: 372 unit/domain/API cases pass (the general 53-case bot suite was not rerun because this change does not touch bots). A separate desktop/mobile/spectator audit verifies offline recovery, presence expiry/recovery, offer cancellation, mutual rematch acceptance, swapped-side play, retained chat identity, spectator synchronization, and seat recovery after reload. No browser errors or horizontal overflow. The local Cloudflare Durable Object accepted exactly one of twelve concurrent rematch acceptances, rejected the other eleven, rejected an old-game move, and accepted the swapped player’s move in the new game.

- Lint, TypeScript, and production build (208 generated pages).
- 395 unit/domain/API cases covered across the full run and targeted reruns. A CPU-sensitive bot deadline check exceeded its 2.8-second bound when run alongside browser/lint work; it passed in isolation. The previous isolated 53-case bot run also passed. Do not describe this as a clean, simultaneous stress/performance pass.
- Six existing browser scenarios pass, including real friend-room creation. The high-strength first-bot-reply deadline similarly needed an isolated rerun after concurrent checks.
- Separate desktop host, mobile guest, and spectator contexts exchanged moves, preserved seats after reload, delivered player chat, and agreed a draw, with no runtime errors or horizontal overflow.
- Cloudflare local Wrangler runtime accepted one of twelve simultaneous same-version moves and rejected the other eleven. State and both seat credentials survived a complete runtime restart; the guest then made the next legal move.
- Mobile/home/setup checks at 320, 390, and 1440 pixels; choice dismissal, board keyboard navigation, manifest icons, and offline navigation fallback passed.
- Blender render and browser 2D/3D boards inspected visually; a move made by raycasting on the 3D board appeared correctly when switching to 2D.

One-off audit runners and screenshots are ignored under `output/playwright/`. No production deployment, remote migration, or live Cloudflare validation is included.

## Next acceptance gates for the active goal

1. Complete Cambodia's online/bot verification and separately resolve referee-dependent championship interpretations. Published digital board/piece counting, stop/restart, counters, and draw adjudication are implemented and tested in `ouk-counting-profile.md`. Keep competitive/friend access gated until the remaining verification is complete.
2. Extend distinct 3D collections and physical tabletop scenes to the other game families, keeping their authentic boards, pieces, and accessible 2D rendering. Cambodia and eight modern chess variants now have real 3D assets. Ancient, East Asian, Thai, draughts, and other families still need their own collections.
3. Exercise friend flows through the deployed Durable Object binding; add account-based contacts and invitation management. Reconnect indicators and coordinated rematches are now implemented. Current invites are bearer links with device-held seat tokens, not an account friends list. Saved account match records and ranked matchmaking still use the existing separate infrastructure.
4. Complete localization of new interface copy, including Khmer, and audit contrast, reduced motion, screen-reader flow, touch gestures, and longer matches across the full catalog.
5. Add saved local matches and resume after closing the app; current cold-start offline play opens a new game. Validate the downloadable pack on physical iOS/Android devices and the deployed host. Chrome cold-start, bot, and 3D validation is complete; browsers can still evict downloaded data.
6. Audit and complete the remaining guide-only games individually. A catalog listing is not evidence of a finished rules engine.

The broad goal remains active. This milestone is reviewable progress, not a claim that every game, mode, or production service is perfected.
